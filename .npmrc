# Public registry so CI (Vercel, GitHub Actions) can resolve packages. Regenerate lockfile if you see artifactory/private URLs in package-lock.json.
registry=https://registry.npmjs.org/
legacy-peer-deps=true
