# Optional: require this exact phrase before the app loads (set the same name/value on your host, e.g. Vercel → Environment Variables → Production).
# Leave unset for no gate (local dev). Do not commit a real phrase in a tracked file.
# VITE_INVITE_CODE=
